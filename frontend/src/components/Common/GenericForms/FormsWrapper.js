import React from 'react';


const FormsWrapper = ({children}) => {
    return (
            <React.Fragment>
                {children}
            </React.Fragment>
    );
};

export default FormsWrapper;