import { basicColors, retroColors } from "style/basic/basicColors";
export const calenderStyle = {
    calender: {
        default: { background: basicColors.fourth, padding: "15px" },
        retro: { background: retroColors.nine, padding: "0" }
    }
}